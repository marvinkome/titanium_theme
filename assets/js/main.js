/**
 Main JS file for theme
 *
 @package Titanium
 */
function myFunction( x ) {
	x.classList.toggle("change");
}

(function ($){
	// Initialize Tooltip.
	$( '[data-toggle="tooltip"]' ).tooltip();
}
(jQuery))